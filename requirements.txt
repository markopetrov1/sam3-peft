# SAM3-PEFT for remote sensing — Python dependencies
# Install PyTorch with CUDA first if using GPU, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

torch>=2.0.0
torchvision>=0.15.0
numpy>=1.21.0
Pillow>=9.0.0
pyyaml>=6.0
tensorboardX>=2.6
tqdm>=4.60.0

# Required by the vendored SAM 3 model code (sam3/). Without these, importing
# sam3.model_builder fails, so training and evaluation cannot start.
iopath>=0.1.9
omegaconf>=2.3.0
hydra-core>=1.3.0
timm>=0.9.0
einops>=0.6.0
ftfy>=6.0.0
decord>=0.6.0
huggingface_hub>=0.20.0

# Used by the dataset preparation, evaluation and analysis scripts under scripts/.
opencv-python>=4.5.0
scipy>=1.7.0
matplotlib>=3.5.0
