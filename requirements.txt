# SAM LoRA for Remote Sensing — Python dependencies
# Install PyTorch with CUDA first if using GPU, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

torch>=2.0.0
torchvision>=0.15.0
numpy>=1.21.0
Pillow>=9.0.0
pyyaml>=6.0
tensorboardX>=2.6
tqdm>=4.60.0
